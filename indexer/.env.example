# Postgres connection string used by both the poller and the API layer.
DATABASE_URL=postgres://localhost:5432/streamfi_indexer

# Interval between poll attempts, in milliseconds.
POLL_INTERVAL_MS=2000

# Max number of events fetched per page.
PAGE_SIZE=100
